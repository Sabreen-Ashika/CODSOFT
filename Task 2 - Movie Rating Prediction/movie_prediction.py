# -*- coding: utf-8 -*-
"""Movie prediction

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/16Zle0IX7rVad037JZxGAdxTc-rJdAr1_
"""

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error

# Load
data = pd.read_excel("IMDb Movies India.xlsx")

# Keep only essential columns
data = data[['Genre', 'Rating']]
data = data.dropna()

# Convert genre to numeric
data = pd.get_dummies(data, columns=['Genre'], drop_first=True)

# Features and label
X = data.drop('Rating', axis=1)
y = data['Rating']

# Train-test split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=1)

# Model
model = LinearRegression()
model.fit(X_train, y_train)

# Predict and evaluate
y_pred = model.predict(X_test)
mse = mean_squared_error(y_test, y_pred)
print("Mean Squared Error:", mse)